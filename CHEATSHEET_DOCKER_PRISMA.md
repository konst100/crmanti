# 🐳 Docker & Prisma Cheatsheet (База данных)

Этот файл — твоя шпаргалка по работе с базой данных. Здесь собраны все команды, которые мы использовали, с объяснением "зачем и почему".

---

## 1. 🚀 Запуск Базы Данных (Docker)
Мы используем Docker, чтобы не устанавливать PostgreSQL прямо в Windows. Это чисто и удобно.

**Где выполнять:** В главной папке проекта (`c:\AntiProject\crmanti`).

*   **Запустить базу (в фоновом режиме):**
    ```bash
    docker compose up -d
    ```
    *Что делает:* Читает файл `docker-compose.yml`, скачивает PostgreSQL и запускает его.

*   **Остановить базу:**
    ```bash
    docker compose down
    ```
    *Что делает:* Выключает и удаляет контейнеры (данные сохраняются в папке `postgres_data`).

*   **Проверить, что запущено:**
    ```bash
    docker ps
    ```
    *Что делает:* Показывает список работающих контейнеров.

---

## 2. 🔗 Работа со схемой (Prisma)
Prisma — это наш "переводчик" с TypeScript на язык базы данных (SQL).

**Где выполнять:** В папке сервера (`c:\AntiProject\crmanti\server`).

*   **Обновить базу после изменения схемы:**
    *(Выполнять каждый раз, когда меняешь файл `schema.prisma`)*
    ```bash
    npx prisma migrate dev --name <имя_изменения>
    ```
    *Пример:* `npx prisma migrate dev --name add_phone_to_user`
    *Что делает:* Сравнивает твой файл `schema.prisma` с реальной базой, создает SQL-файл с изменениями и применяет его.

*   **Открыть визуальный редактор (Prisma Studio):**
    ```bash
    npx prisma studio
    ```
    *Что делает:* Запускает сайт (localhost:5555), где можно мышкой добавлять и смотреть данные в таблицах.

*   **Сгенерировать TypeScript типы (если VS Code не видит новые поля):**
    ```bash
    npx prisma generate
    ```
    *Что делает:* Обновляет библиотеку `@prisma/client`, чтобы автодополнение знало о новых полях.

---

## 3. 🛠 Полезные ссылки
*   **PgAdmin (Админка базы):** [http://localhost:5050](http://localhost:5050)
    *   Login: `admin@admin.com`
    *   Password: `admin`
*   **Prisma Studio:** [http://localhost:5555](http://localhost:5555)

---

## ⚠️ Если что-то сломалось (Troubleshooting)

**Ошибка: "Prisma Client has not been initialized yet"**
*Решение:* Выполни `npx prisma generate`.

**Ошибка: "P1001: Can't reach database server"**
*Решение:* Убедись, что Docker запущен (`docker ps`). Если нет — сделай `docker compose up -d`.

**Ошибка: "Authentication failed"**
*Решение:* Проверь файл `.env` в папке `server`. Пароль там должен совпадать с паролем в `docker-compose.yml`.
