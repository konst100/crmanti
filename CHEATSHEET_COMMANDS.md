# 📜 Шпаргалка команд (Cheatsheet)

Здесь собраны все команды, которые понадобятся для работы над нашими проектами.

---

## 🌍 Project 1: Antigravity (Landing Page)
**Папка:** `c:\antigravity`

### 🚀 Запуск и Разработка
*   **Запустить сайт локально:**
    ```bash
    npm run dev
    ```
    *(Сайт откроется по адресу http://localhost:5173)*

*   **Собрать проект (Build) для проверки ошибок:**
    ```bash
    npm run build
    ```

### ☁️ AWS (Деплой)
Мы настроили **автоматический деплой**.
*   **Как обновить сайт в интернете:**
    Просто сделайте `git push`. AWS Amplify сам увидит изменения, соберет проект и обновит сайт.

---

## 💼 Project 2: Freelance CRM (Full Stack)
**Папка:** `c:\AntiProject\crmanti`

### 🛠 Backend (Сервер - NestJS)
**Папка:** `c:\AntiProject\crmanti\server`

*   **Запустить сервер (режим разработки):**
    ```bash
    npm run start:dev
    ```
    *(Сервер будет доступен на http://localhost:3000)*

*   **Создать новый ресурс (например, Users):**
    ```bash
    npx nest g resource users
    ```

### 🎨 Frontend (Клиент - React)
**Папка:** `c:\AntiProject\crmanti\client`

*   **Запустить клиент:**
    ```bash
    npm run dev
    ```
    *(Клиент будет доступен на http://localhost:5173)*

### 🗄 Database (База данных - Docker)
*   **Запустить базу данных:**
    ```bash
    docker compose up -d
    ```
    *   **PgAdmin (Управление базой):** [http://localhost:5050](http://localhost:5050)
    *   **Login:** `admin@admin.com`
    *   **Password:** `admin`

### 🔗 Связь (Prisma ORM)
**Папка:** `c:\AntiProject\crmanti\server`

*   **Установка Prisma:**
    ```bash
    npm install prisma --save-dev
    npx prisma init
    ```
*   **Обновить базу данных (после изменения схемы):**
    ```bash
    npx prisma migrate dev --name init
    ```
*   **Открыть редактор базы (Prisma Studio):**
    ```bash
    npx prisma studio
    ```

---

## 🐙 Git (Работа с версиями)
Эти команды работают в любой папке проекта.

1.  **Проверить статус (что изменилось):**
    ```bash
    git status
    ```

2.  **Добавить все изменения в "коробку" (Stage):**
    ```bash
    git add .
    ```

3.  **Запаковать "коробку" (Commit):**
    ```bash
    git commit -m "Описание того, что сделали"
    ```
    *Пример: `git commit -m "Добавил страницу контактов"`*

4.  **Отправить на сервер (GitHub & AWS):**
    ```bash
    git push
    ```

5.  **Получить обновления (если работали с другого компа):**
    ```bash
    git pull
    ```

---

## 💡 Полезные советы
*   **Остановка сервера:** Нажмите `Ctrl + C` в терминале.
*   **Очистка консоли:** Напишите `clear` (или `cls` в Windows).
*   **История команд:** Нажимайте `Стрелку Вверх` ⬆️, чтобы повторить прошлые команды.
